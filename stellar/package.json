{
  "name": "medvault-zkp-stellar",
  "version": "1.0.0",
  "description": "ZKP circuits and prover for MedVault on Stellar — BLS12-381 Groth16",
  "type": "module",
  "scripts": {
    "setup": "bash setup_stellar.sh",
    "test": "node test/test_stellar_proof.mjs",
    "compile": "circom circuits/merkle_membership_stellar.circom --r1cs --wasm --sym --prime bls12381 -o build"
  },
  "dependencies": {
    "circomlibjs": "^0.1.7",
    "snarkjs": "^0.7.5"
  }
}
